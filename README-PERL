
Hi all

There is a very simple perl implementation in perl directory. It
uses the s and pm features.

To build it
1- Build and install libswish-e.a (read README-LIB)
2- Execute:
     perl Makefile.PL
     make
     make install

There is also a very simple test.pl program. 

Aug 2000
Jose Ruiz 
jmruiz@boe.es
